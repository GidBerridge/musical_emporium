<!-- <h1>show list of guitars etc</h1> -->



    <div class="container" style="width: 200rem; margin-top: 32px;">
      <h2> Emporium finds for <%= params[:query] %></h2>
      <div class="d-flex justify-content-around">
      <!-- <div class="row row-cols-1 row-cols-md-3"> -->
        <!-- <div class="col"> -->
          <% @instruments.each do |instrument| %>
          <div class="card-product">
            <!-- to do image link here -->
            <p><%= image_tag instrument.image_url %></p>
            <div class="card-body">
              <h4><%= link_to "#{instrument.make + ' ' + instrument.instrument_name}", instrument_path(instrument) %></h4>
              <!-- <p class="card-text"><%= instrument.instrument_name %></p> -->
              <p><%= instrument.delivery_option %></p>
              <p><%= instrument.daily_rate %></p>
            </div>
          </div>
          <% end %>
        </div>
      <!-- </div> -->
      <!-- <%= link_to 'Add a new list', new_instrument_path, class: "btn btn-primary" %> -->


      <%= link_to 'Search again', root_path, class: "btn btn-primary" %>
    </div>

</div>

